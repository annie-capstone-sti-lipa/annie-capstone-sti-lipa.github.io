class UserInfo {
  name: string;
  bio: string;
  id: string;

  constructor(name: string, bio: string, id: string) {
    this.name = name;
    this.bio = bio;
    this.id = id;
  }
}

export default UserInfo;
